{
  "name": "my-app",
  "version": "0.1.2",
  "private": true,
  "homepage": "https://feantuns.github.io/pipeline-on/",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build",
  "dependencies": {
    "@testing-library/cypress": "^9.0.0",
    "cypress": "^12.17.4",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "3.4.1",
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts build",
    "serve": "serve -s build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "react-scripts test --env=jsdom --coverage",
    "eject": "react-scripts eject",
    "e2e": "cypress run"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
